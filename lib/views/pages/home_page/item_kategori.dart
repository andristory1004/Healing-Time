import 'package:flutter/material.dart';

class ItemKategori extends StatelessWidget {
  const ItemKategori({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Item Kategori"),
      ),
    );
  }
}
